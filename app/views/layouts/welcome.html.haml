!!!
%html
  %head
    %title
      \&#x2182
    = stylesheet_link_tag 'common', 'welcome'
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    #container
      #nav-header
        #logo
          %a{:href => root_path}
            10,000
        #nav-right
          .nav-item
            %a
              sign in with
          .nav-item
            %a#twitter_login{:href => '/auth/twitter'}
              twitter
      #content
        - flash.each do |key, value|
          %div{:class => "flash #{key}"}
            = value
        =yield
      #footer
        = render 'common/locales'
